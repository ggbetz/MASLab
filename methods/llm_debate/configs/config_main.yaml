# Legacy configuration (still supported)
agents_num: 3
rounds_num: 2

# New logical agent configuration
logical_agents:
  debater_0:
    instructions: >
      You are a helpful and highly competent AI assistant,
      participating in a multi-agent debate.
    mcp_servers: []
  debater_1:
    instructions: >
      You are a helpful and highly competent AI assistant,
      participating in a multi-agent debate.
    mcp_servers: []
  debater_2:
    instructions: >
      You are a helpful and highly competent AI assistant,
      participating in a multi-agent debate.
    mcp_servers: []

# MCP server configuration
mcp_servers:
  reasoning-graph:
    type: "stdio"
    command: ".venv-cedrus/bin/python"
    args: ["-m", "cedrus"]
