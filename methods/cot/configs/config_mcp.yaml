# methods/cot/configs/config_mcp.yaml

# New logical agent configuration
logical_agents:
  default:
    # Optional but recommended: make instructions explicit
    instructions: >
      You are a helpful chain-of-thought reasoner. Use the reasoning-graph
      MCP tools to structure your internal thinking.
    mcp_servers: ["reasoning-graph"]

# MCP server configuration
mcp_servers:
  reasoning-graph:
    type: "stdio"
    command: ".venv-cedrus/bin/python"
    args: ["-m", "cedrus"]
