parallel_num: 5

# New logical agent configuration
logical_agents:
  default:
    # Optional but recommended: make instructions explicit
    instructions: >
      You are a helpful and highly proficient AI agent. Use the `reasoning-graph`
      MCP tools to structure your internal thinking and to deliberate about decisions.
    mcp_servers: ["reasoning-graph"]

# MCP server configuration
mcp_servers:
  reasoning-graph:
    type: "stdio"
    command: ".venv-cedrus/bin/python"
    args: ["-m", "cedrus"]
